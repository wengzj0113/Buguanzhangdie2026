# K线范围止盈止损模式优化方案与实施计划

## 一、这次修改的目标

在现有“不管涨跌”EA基础上，新增止盈止损距离计算模式。用户可以选择固定距离模式或上一根已完成K线范围模式，并继续使用已有的三种六单循环模式。

## 二、止盈止损模式

### 模式一：固定距离模式

保持现有行为：

- 止损距离由用户设置，默认500点；
- 止盈距离由用户设置，默认500点；
- 不检查K线长度；
- 原有三种循环模式和手数倍增逻辑不变。

### 模式二：上一根K线范围模式

使用当前图表周期的上一根已完成K线：

```text
K线范围点数 = (上一根K线最高价 - 上一根K线最低价) / Point
```

用户设置两个边界：

- 最小K线范围：默认500点；
- 最大K线范围：默认1000点。

只有满足以下条件时，K线才有效：

```text
最小范围 <= K线范围 <= 最大范围
```

边界值包含在有效范围内：等于最小值或最大值都允许。

如果当前没有活动循环，且K线范围小于最小值或大于最大值，则该K线无效，不启动首单；EA等待下一根已完成K线重新检查。活动循环一旦确定了距离，后续不再因为K线范围变化而中断。

动态模式下，最终止损距离和止盈距离使用同一个有效K线范围。

## 三、与六单循环的结合

原有循环模式保持不变：

| 模式 | 首单多 | 首单空 |
|---|---|---|
| 模式一 | BUY, SELL, SELL, BUY, SELL, SELL | SELL, BUY, BUY, SELL, BUY, BUY |
| 模式二 | BUY, SELL, BUY, SELL, BUY, BUY | SELL, BUY, SELL, BUY, SELL, SELL |
| 模式三 | BUY, SELL, BUY, BUY, SELL, BUY | SELL, BUY, SELL, SELL, BUY, SELL |

每次新订单建立后，仍然按照循环序号计算下一单方向，手数仍然是当前实际手数乘以用户设置的倍数。

在动态K线模式下，只有启动新循环时检查上一根已完成K线：

- K线有效：确定本轮止盈止损距离并启动循环；
- K线无效：暂不创建首单，下一根K线重新检查。

当前已经存在的持仓和已创建的预挂单，其止损和止盈不因后来K线范围变化而移动；距离在本轮建立时确定，后续预挂单和止损切换沿用该距离。

## 四、挂单和K线过滤的处理建议

下一单价格仍然取当前订单止损价，挂单类型仍按目标方向和价格位置自动选择：

- 反向多/空衔接使用Buy Stop或Sell Stop；
- 同向多/空衔接使用Buy Limit或Sell Limit。

推荐规则是：启动循环时检查K线范围并确定距离；后续挂单沿用当前持仓已确定的距离，并继续保留直到成交、止盈清理或被EA替换。这样不会因为下一根K线范围变化而撤掉或阻断已经属于当前循环的保护挂单。

如果用户希望严格禁止挂单在无效K线中成交，需要另行增加“无效K线撤销挂单”开关；当前版本不启用该行为，避免循环被后来K线范围中断。

## 五、参数设计

新增：

- `InpDistanceMode`：固定距离 / K线范围；默认固定距离；
- `InpCandleMinRangePoints`：最小K线范围，默认500点；
- `InpCandleMaxRangePoints`：最大K线范围，默认1000点。

保留：

- `InpStopLossDistancePoints`；
- `InpTakeProfitDistancePoints`；
- `InpFirstDirection`；
- `InpCycleMode`，支持模式一、模式二、模式三；
- `InpInitialLots`；
- `InpReverseMultiplier`；
- `InpMagicNumber`；
- `InpOrderComment`。

参数校验：

- 所有距离必须大于0；
- 最大K线范围必须大于或等于最小K线范围；
- 如果动态模式取不到上一根已完成K线数据，则不创建新单并记录日志；
- 止损和止盈距离按品种Point计算并进行价格精度归一化。

## 六、实施步骤

### 第1步：扩展逻辑模型测试

在Python模型中先增加：

1. 固定模式仍使用用户的SL/TP距离；
2. K线范围等于最小值时允许；
3. K线范围等于最大值时允许；
4. K线范围小于最小值时跳过开单；
5. K线范围大于最大值时跳过开单；
6. 动态模式有效时，止损和止盈使用相同K线范围；
7. 三种循环模式×两种首单方向共六种组合均能完成六单循环；
8. 连续同向时仍使用Buy Limit或Sell Limit；
9. 止盈后清除挂单并结束本轮。

先运行失败测试，再实现最小模型，最后运行完整测试。

### 第2步：修改MT5 EA

修改 `NoMatterRiseFall_MT5.mq5`：

1. 新增距离模式枚举和最小/最大K线范围输入；
2. 新增上一根已完成K线范围计算函数；
3. 新增统一的距离函数，固定模式返回固定参数，动态模式在启动时返回有效K线范围或“无效”；
4. 首单创建前检查距离有效性；
5. 后续创建挂单和止损切换沿用当前持仓已确定的距离，不重复检查实时K线范围；
6. 用实际成交价和当前循环已确定的距离设置新订单SL/TP；
7. 保留三种六单循环、循环索引持久化和订单防重逻辑；
8. 编译并检查0错误、0警告。

### 第3步：同步修改MT4 EA

修改 `NoMatterRiseFall_MT4.mq4`，保持与MT5完全一致：

1. 相同的距离模式和参数；
2. 相同的上一根K线范围过滤；
3. 相同的最小/最大边界规则；
4. 相同的六单循环方向和手数规则；
5. 相同的挂单类型判断；
6. 编译并检查0错误、0警告。

### 第4步：回归验证

执行：

- `python -m pytest -q`；
- MT5 MetaEditor编译；
- MT4 MetaEditor编译；
- 源码静态检查，确认模式三和距离模式均存在。

验证场景：

| 场景 | 预期结果 |
|---|---|
| 固定模式，任意K线范围 | 按固定SL/TP开单 |
| 动态模式，范围300，最小500 | 不开新单 |
| 动态模式，范围500 | 允许，使用500点 |
| 动态模式，范围700 | 允许，使用700点 |
| 动态模式，范围1000 | 允许，使用1000点 |
| 动态模式，范围1200，最大1000 | 不启动新循环 |
| 循环运行中后来K线范围300 | 仍按当前循环已确定的距离继续加单 |
| 动态模式有效，连续同向循环 | 使用对应Limit挂单 |
| 止盈 | 删除剩余挂单并结束本轮 |

## 七、验收标准

- 用户可以选择固定距离或K线范围模式；
- 启动新循环时，K线范围小于最小值或大于最大值时不创建首单；
- 循环运行中K线范围变化不影响后续预挂单、止损切换和加仓；
- K线范围等于边界值时允许创建新单；
- 动态模式的止盈和止损距离均使用有效K线范围；
- 三种六单循环模式继续正常工作；
- MT5和MT4行为一致；
- Python测试全部通过；
- MT5和MT4编译均为0错误、0警告。
